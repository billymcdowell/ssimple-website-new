export interface FeatureProps {
  iconUrl: string;
  title: string;
  description: string;
}

export interface StatProps {
  value: string;
  label: string;
}

export interface StepProps {
  number: string;
  title: string;
  description: string;
  iconUrl: string;
}